// variables
let price = 100,
    fees = 10,
    times = 2,
    finalPrice = 0;

// Finding the price after the fees
finalPrice = price + price * (fees / 100) * times;

// displying finalPrice
console.log(finalPrice);
